@model Exemplo.Models.Contrato

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Tem a certeza que deseja apagar isto?</h3>
<div>
    <h4>Contrato</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NomeFornecedor)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NomeFornecedor)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.PedidoCompra)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PedidoCompra)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.NºProcedimento)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NºProcedimento)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Apagar" class="btn btn-default" /> 
            <a class="btn btn-primary" href="@Url.Action("Index")"><span class="glyphicon glyphicon-arrow-left"></span></a>
        </div>
    }
</div>
